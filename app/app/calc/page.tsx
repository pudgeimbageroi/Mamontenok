import { PageShell } from "@/components/page-shell";

export default function CalcPage() {
  return (
    <PageShell
      title="Калькулятор"
      subtitle="Курсы валют, формирование цены и расчёт сделки"
      sprint="2"
    />
  );
}
